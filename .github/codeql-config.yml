name: "Python CodeQL Config"

paths:
  - snowflake_ingestion
  - dbt_transformations

paths-ignore:
  - nyc_taxi_docs
  - .github

queries:
  - uses: security-and-quality
    exclude:
      - id: py/namespace-pollution
  - uses: security-extended